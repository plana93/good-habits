<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <!-- TextureView per l'output della fotocamera -->
    <TextureView
        android:id="@+id/textureView"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <!-- ImageView per l'output elaborato -->
    <ImageView
        android:id="@+id/imageView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="#000" />

    <!-- Bordi -->
    <View
        android:id="@+id/topBorder"
        android:layout_width="match_parent"
        android:layout_height="6dp"
        android:layout_alignParentTop="true"
        android:background="@android:color/transparent" />

    <View
        android:id="@+id/bottomBorder"
        android:layout_width="match_parent"
        android:layout_height="6dp"
        android:layout_alignParentBottom="true"
        android:background="@android:color/transparent" />

    <View
        android:id="@+id/leftBorder"
        android:layout_width="6dp"
        android:layout_height="match_parent"
        android:layout_alignParentStart="true"
        android:layout_marginTop="16dp"
        android:layout_marginBottom="16dp"
        android:background="@android:color/transparent" />

    <View
        android:id="@+id/rightBorder"
        android:layout_width="6dp"
        android:layout_height="match_parent"
        android:layout_alignParentEnd="true"
        android:layout_marginTop="16dp"
        android:layout_marginBottom="16dp"
        android:background="@android:color/transparent" />

    <!-- TextView per il numero -->
    <TextView
        android:id="@+id/numberTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_alignParentEnd="true"
        android:layout_marginTop="8dp"
        android:layout_marginEnd="8dp"
        android:background="@drawable/rounded_corner_background"
        android:elevation="4dp"
        android:fontFamily="monospace"
        android:padding="8dp"
        android:text=""
        android:textAlignment="center"
        android:textColor="@android:color/white"
        android:textSize="16sp"
        android:lineSpacingExtra="1dp"
        android:letterSpacing="0.05" />
    <!-- Font monospace tipo terminale, dimensione ridotta -->

    <!-- Pulsante FINE per terminare sessione AI -->
    <Button
        android:id="@+id/btn_finish_ai_session"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_alignParentStart="true"
        android:layout_marginTop="8dp"
        android:layout_marginStart="8dp"
        android:text="✕ FINE"
        android:textColor="@android:color/white"
        android:backgroundTint="#FF4444"
        android:textSize="14sp"
        android:fontFamily="sans-serif-medium"
        android:elevation="4dp"
        android:visibility="gone" />

    <!-- Indicatore di progresso per attivazione -->
    <TextView
        android:id="@+id/activationProgress"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:background="@drawable/rounded_corner_background"
        android:elevation="4dp"
        android:fontFamily="monospace"
        android:padding="16dp"
        android:text="Rilevamento posizione...\n⬤⬤⬤⬤⬤⬤⬤⬤"
        android:textAlignment="center"
        android:textColor="@android:color/white"
        android:textSize="18sp"
        android:visibility="gone" />

    <!-- Recovery Mode Banner (Phase 4) -->
    <TextView
        android:id="@+id/recoveryBanner"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_marginTop="60dp"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="16dp"
        android:background="#FF2196F3"
        android:elevation="8dp"
        android:fontFamily="sans-serif-medium"
        android:padding="16dp"
        android:text="🔄 MODALITÀ RECUPERO\nData: --\nRipetizioni richieste: 50"
        android:textAlignment="center"
        android:textColor="@android:color/white"
        android:textSize="16sp"
        android:visibility="gone" />

    <!-- Bottone EXIT per chiudere e copiare negli appunti (visibile solo in modalità recording) -->
    <Button
        android:id="@+id/btn_exit_and_copy"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="EXIT &amp; Copy"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="16dp"
        android:visibility="gone"
        android:backgroundTint="#FF0000" />

</RelativeLayout>
